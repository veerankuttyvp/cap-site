/* Controllers */
angular.module('cap.controllers.questionnaire', []).

controller('QuestionnaireCtrl', ['$scope', '$element', '$http', '$timeout', '$window', '$cookies',
    function($scope, $element, $http, $timeout, $window, $cookies) {
    }
]);
