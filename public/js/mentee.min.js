/*! cap-website 2014-08-15 */
"use strict";

var app = angular.module("cap", [ "ngAnimate", "ngCookies", "cap.controllers", "cap.controllers.mentee", "cap.filters", "cap.router", "cap.services", "cap.directives" ]);

app.config(function($interpolateProvider) {
    $interpolateProvider.startSymbol("[[");
    $interpolateProvider.endSymbol("]]");
});

app.config([ function() {} ]);

angular.module("cap.controllers.mentee", []).controller("MenteeCtrl", [ "$scope", "$element", "$http", "$timeout", "$window", "$cookies", function($scope, $element, $http, $timeout, $window, $cookies) {} ]);